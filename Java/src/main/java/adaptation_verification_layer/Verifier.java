package adaptation_verification_layer;

import specification.AdaptationGoal;
import specification.SimulationResult;
import specification.VerificationResult;

import java.util.ArrayList;

public class Verifier {
    //todo: specify your goal checker
    public Verifier() {

    }

    public VerificationResult verify(ArrayList<SimulationResult> simResults, AdaptationGoal goal) {
        //todo: return verification results based on the given simulation results
        VerificationResult veriResult = new VerificationResult();
        return veriResult;
    }
}
